module.exports={
    DB_NAME:"ecomm_db",
    DB_URL:"mongodb://localhost/ecomm_db"   //if sometimes "localhost" isn't working then we can write "0.0.0.0"
    //i did one mistake here i.e i wrote mongoose instead of mongodb on just upper  line--------------
}